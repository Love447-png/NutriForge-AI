[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn app.main:create_app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:${PORT:-8000}"
healthcheckUrl = "/api/health"
restartPolicyMaxRetries = 5
